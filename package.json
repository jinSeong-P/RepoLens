{
  "name": "repolens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-only",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.strict.json --noEmit",
    "test": "tsx --test test/*.test.ts",
    "build": "npm run build:chrome",
    "build:chrome": "node scripts/build-extension.mjs chrome",
    "build:firefox": "node scripts/build-extension.mjs firefox",
    "check": "npm run typecheck && npm test && npm run build:chrome && npm run build:firefox && node scripts/check-manifest.mjs build/extension chrome && node scripts/check-manifest.mjs build/firefox-extension firefox",
    "package:extension": "npm run package:chrome",
    "package:chrome": "npm run build:chrome && node scripts/package-extension.mjs build/extension chrome",
    "package:firefox": "npm run build:firefox && node scripts/package-extension.mjs build/firefox-extension firefox",
    "package:all": "npm run package:chrome && npm run package:firefox"
  },
  "devDependencies": {
    "@types/chrome": "0.2.5",
    "@types/node": "22.20.0",
    "tsx": "4.23.12",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22"
  }
}
